#!/bin/bash

# This script is run on box boot as root. I mean it's basically RCE as a service but y'know whatever.

# Wait until we have an internet connection.


# Run the orchestrator container.
docker run 

# Update the script for the next boot.